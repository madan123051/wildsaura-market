"use client";\n\nimport { useState, useRef } from "react";\nimport Image from "next/image";\nimport { useRouter } from "next/navigation";\nimport {\n  EQUIPMENT_CATEGORIES,\n  type EquipmentCategory,\n  type EquipmentCondition,\n} from "@/types";\n\ninterface AIRecognitionResult {\n  itemName: string;\n  category: EquipmentCategory;\n  priceRange: { min: number; max: number };\n  confidence: number;\n  suggestions: string[];\n}\n\ninterface FormData {\n  title: string;\n  description: string;\n  category: EquipmentCategory;\n  price: string;\n  condition: EquipmentCondition;\n  brand: string;\n  model: string;\n  yearPurchased: string;\n  location: string;\n  tags: string[];\n  images: File[];\n  imagePreviews: string[];\n}\n\nexport default function SellPage() {\n  const router = useRouter();\n  const fileInputRef = useRef<HTMLInputElement>(null);\n  const [step, setStep] = useState<1 | 2 | 3>(1);\n  const [isAnalyzing, setIsAnalyzing] = useState(false);\n  const [aiResult, setAiResult] = useState<AIRecognitionResult | null>(null);\n  const [tagInput, setTagInput] = useState(\"\");\n  const [isSubmitting, setIsSubmitting] = useState(false);\n  const [error, setError] = useState(\"\");\n  const [success, setSuccess] = useState(\"\");\n\n  const [form, setForm] = useState<FormData>({\n    title: \"\",\n    description: \"\",\n    category: \"other\",\n    price: \"\",\n    condition: \"used\",\n    brand: \"\",\n    model: \"\",\n    yearPurchased: \"\",\n    location: \"\",\n    tags: [],\n    images: [],\n    imagePreviews: [],\n  });\n\n  // Handle image upload\n  const handleImageUpload = async (e: React.ChangeEvent<HTMLInputElement>) => {\n    const files = e.target.files;\n    if (!files) return;\n\n    const newImages = Array.from(files);\n    const newPreviews = newImages.map((file) => URL.createObjectURL(file));\n\n    setForm((prev) => ({\n      ...prev,\n      images: [...prev.images, ...newImages],\n      imagePreviews: [...prev.imagePreviews, ...newPreviews],\n    }));\n\n    // Auto-analyze first image\n    if (newImages.length > 0 && !aiResult) {\n      await analyzeImage(newImages[0]);\n    }\n  };\n\n  // AI Recognition - Analyze first image\n  const analyzeImage = async (file: File) => {\n    setIsAnalyzing(true);\n    try {\n      // Simulate AI analysis (in real app, send to backend API)\n      // For now, we'll use placeholder logic\n      await new Promise((resolve) => setTimeout(resolve, 2000));\n\n      // Mock AI result based on file name or detected features\n      const mockResult: AIRecognitionResult = {\n        itemName: \"Professional Digital Camera\",\n        category: \"camera\",\n        priceRange: { min: 45000, max: 80000 },\n        confidence: 0.92,\n        suggestions: [\n          \"Check if all accessories are included\",\n          \"Mention warranty status if applicable\",\n          \"Include shutter count for cameras\",\n          \"Describe any cosmetic damage clearly\",\n        ],\n      };\n\n      setAiResult(mockResult);\n      setForm((prev) => ({\n        ...prev,\n        title: mockResult.itemName,\n        category: mockResult.category,\n        price: String(mockResult.priceRange.min),\n      }));\n      setStep(2);\n    } catch (err) {\n      setError(\"Failed to analyze image. Please try again.\");\n    } finally {\n      setIsAnalyzing(false);\n    }\n  };\n\n  // Remove image\n  const removeImage = (index: number) => {\n    setForm((prev) => ({\n      ...prev,\n      images: prev.images.filter((_, i) => i !== index),\n      imagePreviews: prev.imagePreviews.filter((_, i) => i !== index),\n    }));\n  };\n\n  // Add tag\n  const addTag = () => {\n    if (tagInput.trim() && form.tags.length < 5) {\n      setForm((prev) => ({\n        ...prev,\n        tags: [...prev.tags, tagInput.trim()],\n      }));\n      setTagInput(\"\");\n    }\n  };\n\n  // Remove tag\n  const removeTag = (index: number) => {\n    setForm((prev) => ({\n      ...prev,\n      tags: prev.tags.filter((_, i) => i !== index),\n    }));\n  };\n\n  // Submit listing\n  const handleSubmit = async (e: React.FormEvent) => {\n    e.preventDefault();\n    setIsSubmitting(true);\n    setError(\"\");\n\n    try {\n      if (!form.title || !form.description || !form.price) {\n        throw new Error(\"Please fill in all required fields\");\n      }\n\n      if (form.images.length === 0) {\n        throw new Error(\"Please upload at least one image\");\n      }\n\n      // Here you would send data to backend\n      // For now, just show success and redirect\n      setSuccess(\"✅ Listing created successfully!\");\n      setTimeout(() => {\n        router.push(\"/shopping\");\n      }, 2000);\n    } catch (err) {\n      setError(err instanceof Error ? err.message : \"Failed to create listing\");\n    } finally {\n      setIsSubmitting(false);\n    }\n  };\n\n  return (\n    <div className=\"min-h-screen bg-gradient-to-b from-blue-50 to-white\">\n      {/* Header */}\n      <div className=\"border-b bg-white sticky top-0 z-10\">\n        <div className=\"max-w-4xl mx-auto px-4 py-4 sm:py-6\">\n          <h1 className=\"text-2xl sm:text-3xl font-bold text-gray-900\">📸 Sell Your Equipment</h1>\n          <p className=\"text-gray-600 mt-1\">Upload photos and let AI identify your item</p>\n        </div>\n      </div>\n\n      <div className=\"max-w-4xl mx-auto px-4 py-8\">\n        {/* Progress Steps */}\n        <div className=\"mb-8 flex justify-between\">\n          {[1, 2, 3].map((s) => (\n            <div key={s} className=\"flex-1\">\n              <div\n                className={`h-1 mb-2 rounded-full transition-colors ${\n                  step >= s ? \"bg-blue-600\" : \"bg-gray-300\"\n                }`}\n              />\n              <p\n                className={`text-sm font-medium ${\n                  step >= s ? \"text-blue-600\" : \"text-gray-500\"\n                }`}\n              >\n                {s === 1 ? \"📷 Upload\" : s === 2 ? \"🔍 Review\" : \"✅ Confirm\"}\n              </p>\n            </div>\n          ))}\n        </div>\n\n        {error && (\n          <div className=\"mb-6 p-4 bg-red-50 border border-red-200 rounded-lg text-red-700\">\n            {error}\n          </div>\n        )}\n\n        {success && (\n          <div className=\"mb-6 p-4 bg-green-50 border border-green-200 rounded-lg text-green-700\">\n            {success}\n          </div>\n        )}\n\n        {/* Step 1: Image Upload */}\n        {step === 1 && (\n          <div className=\"space-y-6\">\n            {/* Upload Area */}\n            <div\n              onClick={() => fileInputRef.current?.click()}\n              className=\"border-2 border-dashed border-blue-300 rounded-2xl p-8 sm:p-12 bg-blue-50 hover:bg-blue-100 cursor-pointer transition-colors text-center group\"\n            >\n              <input\n                ref={fileInputRef}\n                type=\"file\"\n                multiple\n                accept=\"image/*\"\n                onChange={handleImageUpload}\n                className=\"hidden\"\n              />\n              <div className=\"text-4xl mb-3 group-hover:scale-110 transition-transform\">📸</div>\n              <h3 className=\"text-lg font-semibold text-gray-900 mb-2\">\n                Drop images here or click to browse\n              </h3>\n              <p className=\"text-gray-600 text-sm mb-4\">Supports JPG, PNG, WebP (Max 5 images)</p>\n              <div className=\"text-xs text-gray-500\">\n                💡 Tip: Clear photos from multiple angles = better sales\n              </div>\n            </div>\n\n            {/* Image Previews */}\n            {form.imagePreviews.length > 0 && (\n              <div className=\"space-y-3\">\n                <h3 className=\"font-semibold text-gray-900\">📸 Uploaded Images ({form.imagePreviews.length})</h3>\n                <div className=\"grid grid-cols-2 sm:grid-cols-3 gap-3\">\n                  {form.imagePreviews.map((preview, i) => (\n                    <div key={i} className=\"relative group\">\n                      <img\n                        src={preview}\n                        alt={`Preview ${i}`}\n                        className=\"w-full h-32 object-cover rounded-lg\"\n                      />\n                      <button\n                        onClick={() => removeImage(i)}\n                        className=\"absolute top-2 right-2 bg-red-500 text-white p-1 rounded opacity-0 group-hover:opacity-100 transition-opacity\"\n                      >\n                        ✕\n                      </button>\n                    </div>\n                  ))}\n                  {form.imagePreviews.length < 5 && (\n                    <button\n                      onClick={() => fileInputRef.current?.click()}\n                      className=\"border-2 border-dashed border-gray-300 rounded-lg h-32 flex items-center justify-center hover:border-blue-500 hover:bg-blue-50 transition-colors\"\n                    >\n                      <span className=\"text-2xl\">+</span>\n                    </button>\n                  )}\n                </div>\n              </div>\n            )}\n\n            {/* Next Button */}\n            <div className=\"flex gap-3\">\n              <button\n                onClick={() => {\n                  if (form.imagePreviews.length > 0) {\n                    analyzeImage(form.images[0]);\n                  } else {\n                    setError(\"Please upload at least one image\");\n                  }\n                }}\n                disabled={form.imagePreviews.length === 0 || isAnalyzing}\n                className=\"flex-1 bg-blue-600 hover:bg-blue-700 disabled:bg-gray-400 text-white font-semibold py-3 rounded-lg transition-colors\"\n              >\n                {isAnalyzing ? \"🤖 Analyzing...\" : \"Continue →\"}\n              </button>\n            </div>\n          </div>\n        )}\n\n        {/* Step 2: Review & Edit */}\n        {step === 2 && (\n          <form onSubmit={(e) => {\n            e.preventDefault();\n            setStep(3);\n          }} className=\"space-y-6\">\n            {/* AI Result Summary */}\n            {aiResult && (\n              <div className=\"bg-gradient-to-r from-blue-50 to-indigo-50 border border-blue-200 rounded-xl p-6\">\n                <div className=\"flex items-start justify-between\">\n                  <div>\n                    <p className=\"text-sm font-medium text-blue-600 mb-1\">🤖 AI Recognition Result</p>\n                    <h3 className=\"text-xl font-bold text-gray-900\">{aiResult.itemName}</h3>\n                    <p className=\"text-sm text-gray-600 mt-2\">\n                      Confidence: <span className=\"font-semibold\">{(aiResult.confidence * 100).toFixed(0)}%</span>\n                    </p>\n                  </div>\n                  <span className=\"text-4xl\">✨</span>\n                </div>\n                <div className=\"mt-4 pt-4 border-t border-blue-200 space-y-2\">\n                  <p className=\"text-sm font-medium text-gray-700\">💡 Seller Tips:</p>\n                  {aiResult.suggestions.map((tip, i) => (\n                    <p key={i} className=\"text-sm text-gray-600\">• {tip}</p>\n                  ))}\n                </div>\n              </div>\n            )}\n\n            {/* Form Fields */}\n            <div className=\"space-y-5 bg-white p-6 rounded-xl border border-gray-200\">\n              {/* Title */}\n              <div>\n                <label className=\"block text-sm font-semibold text-gray-900 mb-2\">Item Name *</label>\n                <input\n                  type=\"text\"\n                  value={form.title}\n                  onChange={(e) => setForm({ ...form, title: e.target.value })}\n                  className=\"w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent\"\n                  placeholder=\"e.g., Canon EOS 6D Mark II\"\n                  required\n                />\n              </div>\n\n              {/* Price & Condition */}\n              <div className=\"grid grid-cols-1 sm:grid-cols-2 gap-4\">\n                <div>\n                  <label className=\"block text-sm font-semibold text-gray-900 mb-2\">Price (NPR) *</label>\n                  <div className=\"relative\">\n                    <span className=\"absolute left-4 top-3 text-gray-600 font-semibold\">₹</span>\n                    <input\n                      type=\"number\"\n                      value={form.price}\n                      onChange={(e) => setForm({ ...form, price: e.target.value })}\n                      className=\"w-full pl-8 pr-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent\"\n                      placeholder=\"45000\"\n                      required\n                    />\n                  </div>\n                </div>\n                <div>\n                  <label className=\"block text-sm font-semibold text-gray-900 mb-2\">Condition *</label>\n                  <select\n                    value={form.condition}\n                    onChange={(e) => setForm({ ...form, condition: e.target.value as EquipmentCondition })}\n                    className=\"w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent\"\n                  >\n                    <option value=\"new\">✨ New</option>\n                    <option value=\"like-new\">⭐ Like New</option>\n                    <option value=\"used\">👍 Used</option>\n                    <option value=\"refurbished\">🔧 Refurbished</option>\n                  </select>\n                </div>\n              </div>\n\n              {/* Category & Brand */}\n              <div className=\"grid grid-cols-1 sm:grid-cols-2 gap-4\">\n                <div>\n                  <label className=\"block text-sm font-semibold text-gray-900 mb-2\">Category *</label>\n                  <select\n                    value={form.category}\n                    onChange={(e) => setForm({ ...form, category: e.target.value as EquipmentCategory })}\n                    className=\"w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent\"\n                  >\n                    {EQUIPMENT_CATEGORIES.map((cat) => (\n                      <option key={cat.value} value={cat.value}>\n                        {cat.icon} {cat.label}\n                      </option>\n                    ))}\n                  </select>\n                </div>\n                <div>\n                  <label className=\"block text-sm font-semibold text-gray-900 mb-2\">Brand</label>\n                  <input\n                    type=\"text\"\n                    value={form.brand}\n                    onChange={(e) => setForm({ ...form, brand: e.target.value })}\n                    className=\"w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent\"\n                    placeholder=\"e.g., Canon\"\n                  />\n                </div>\n              </div>\n\n              {/* Model & Year */}\n              <div className=\"grid grid-cols-1 sm:grid-cols-2 gap-4\">\n                <div>\n                  <label className=\"block text-sm font-semibold text-gray-900 mb-2\">Model</label>\n                  <input\n                    type=\"text\"\n                    value={form.model}\n                    onChange={(e) => setForm({ ...form, model: e.target.value })}\n                    className=\"w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent\"\n                    placeholder=\"e.g., 6D Mark II\"\n                  />\n                </div>\n                <div>\n                  <label className=\"block text-sm font-semibold text-gray-900 mb-2\">Year Purchased</label>\n                  <input\n                    type=\"number\"\n                    value={form.yearPurchased}\n                    onChange={(e) => setForm({ ...form, yearPurchased: e.target.value })}\n                    className=\"w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent\"\n                    placeholder=\"2020\"\n                    min=\"1990\"\n                    max={new Date().getFullYear()}\n                  />\n                </div>\n              </div>\n\n              {/* Location */}\n              <div>\n                <label className=\"block text-sm font-semibold text-gray-900 mb-2\">Location</label>\n                <input\n                  type=\"text\"\n                  value={form.location}\n                  onChange={(e) => setForm({ ...form, location: e.target.value })}\n                  className=\"w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent\"\n                  placeholder=\"e.g., Kathmandu\"\n                />\n              </div>\n\n              {/* Description */}\n              <div>\n                <label className=\"block text-sm font-semibold text-gray-900 mb-2\">Description *</label>\n                <textarea\n                  value={form.description}\n                  onChange={(e) => setForm({ ...form, description: e.target.value })}\n                  className=\"w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent resize-none\"\n                  placeholder=\"Describe the condition, any damage, accessories included, etc.\"\n                  rows={4}\n                  required\n                />\n              </div>\n\n              {/* Tags */}\n              <div>\n                <label className=\"block text-sm font-semibold text-gray-900 mb-2\">Tags (up to 5)</label>\n                <div className=\"flex gap-2 mb-3\">\n                  <input\n                    type=\"text\"\n                    value={tagInput}\n                    onChange={(e) => setTagInput(e.target.value)}\n                    onKeyPress={(e) => e.key === \"Enter\" && (e.preventDefault(), addTag())}\n                    className=\"flex-1 px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent\"\n                    placeholder=\"Add a tag\"\n                  />\n                  <button\n                    type=\"button\"\n                    onClick={addTag}\n                    className=\"px-4 py-2 bg-gray-200 hover:bg-gray-300 rounded-lg font-medium\"\n                  >\n                    Add\n                  </button>\n                </div>\n                <div className=\"flex flex-wrap gap-2\">\n                  {form.tags.map((tag, i) => (\n                    <span\n                      key={i}\n                      className=\"bg-blue-100 text-blue-700 px-3 py-1 rounded-full text-sm flex items-center gap-2\"\n                    >\n                      {tag}\n                      <button\n                        type=\"button\"\n                        onClick={() => removeTag(i)}\n                        className=\"hover:text-blue-900\"\n                      >\n                        ✕\n                      </button>\n                    </span>\n                  ))}\n                </div>\n              </div>\n            </div>\n\n            {/* Buttons */}\n            <div className=\"flex gap-3\">\n              <button\n                type=\"button\"\n                onClick={() => setStep(1)}\n                className=\"flex-1 px-6 py-3 border border-gray-300 text-gray-700 font-semibold rounded-lg hover:bg-gray-50 transition-colors\"\n              >\n                ← Back\n              </button>\n              <button\n                type=\"submit\"\n                className=\"flex-1 px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white font-semibold rounded-lg transition-colors\"\n              >\n                Review → \n              </button>\n            </div>\n          </form>\n        )}\n\n        {/* Step 3: Confirm */}\n        {step === 3 && (\n          <form onSubmit={handleSubmit} className=\"space-y-6\">\n            {/* Preview */}\n            <div className=\"bg-white rounded-xl border border-gray-200 overflow-hidden\">\n              {/* Images */}\n              <div className=\"relative h-64 sm:h-96 bg-gray-100\">\n                <img\n                  src={form.imagePreviews[0]}\n                  alt=\"Preview\"\n                  className=\"w-full h-full object-cover\"\n                />\n                {form.imagePreviews.length > 1 && (\n                  <div className=\"absolute top-3 right-3 bg-black/50 text-white px-3 py-1 rounded-full text-sm font-semibold\">\n                    +{form.imagePreviews.length - 1} more\n                  </div>\n                )}\n              </div>\n\n              {/* Details */}\n              <div className=\"p-6\">\n                <h2 className=\"text-2xl font-bold text-gray-900\">{form.title}</h2>\n                <p className=\"text-3xl font-bold text-blue-600 mt-2\">₹{Number(form.price).toLocaleString(\"en-NP\")}</p>\n                <p className=\"text-gray-600 mt-3\">{form.description}</p>\n\n                <div className=\"grid grid-cols-2 gap-4 mt-6 pt-6 border-t border-gray-200\">\n                  <div>\n                    <p className=\"text-sm text-gray-600\">Category</p>\n                    <p className=\"font-semibold text-gray-900\">\n                      {EQUIPMENT_CATEGORIES.find((c) => c.value === form.category)?.label}\n                    </p>\n                  </div>\n                  <div>\n                    <p className=\"text-sm text-gray-600\">Condition</p>\n                    <p className=\"font-semibold text-gray-900 capitalize\">{form.condition}</p>\n                  </div>\n                  {form.brand && (\n                    <div>\n                      <p className=\"text-sm text-gray-600\">Brand</p>\n                      <p className=\"font-semibold text-gray-900\">{form.brand}</p>\n                    </div>\n                  )}\n                  {form.location && (\n                    <div>\n                      <p className=\"text-sm text-gray-600\">Location</p>\n                      <p className=\"font-semibold text-gray-900\">{form.location}</p>\n                    </div>\n                  )}\n                </div>\n\n                {form.tags.length > 0 && (\n                  <div className=\"mt-6 pt-6 border-t border-gray-200\">\n                    <p className=\"text-sm text-gray-600 mb-2\">Tags</p>\n                    <div className=\"flex flex-wrap gap-2\">\n                      {form.tags.map((tag, i) => (\n                        <span\n                          key={i}\n                          className=\"bg-gray-100 text-gray-700 px-3 py-1 rounded-full text-sm\"\n                        >\n                          {tag}\n                        </span>\n                      ))}\n                    </div>\n                  </div>\n                )}\n              </div>\n            </div>\n\n            {/* Checklist */}\n            <div className=\"bg-yellow-50 border border-yellow-200 rounded-lg p-4\">\n              <p className=\"font-semibold text-yellow-900 mb-3\">✓ Before Publishing:</p>\n              <ul className=\"space-y-2 text-sm text-yellow-800\">\n                <li>✓ All prices are in Nepali Rupees (NPR)</li>\n                <li>✓ Images clearly show the item condition</li>\n                <li>✓ Description is honest and detailed</li>\n                <li>✓ You'll respond to buyer inquiries quickly</li>\n              </ul>\n            </div>\n\n            {/* Buttons */}\n            <div className=\"flex gap-3\">\n              <button\n                type=\"button\"\n                onClick={() => setStep(2)}\n                className=\"flex-1 px-6 py-3 border border-gray-300 text-gray-700 font-semibold rounded-lg hover:bg-gray-50 transition-colors\"\n              >\n                ← Edit\n              </button>\n              <button\n                type=\"submit\"\n                disabled={isSubmitting}\n                className=\"flex-1 px-6 py-3 bg-green-600 hover:bg-green-700 disabled:bg-gray-400 text-white font-semibold rounded-lg transition-colors\"\n              >\n                {isSubmitting ? \"🚀 Publishing...\" : \"🎉 Publish Listing\"}\n              </button>\n            </div>\n          </form>\n        )}\n      </div>\n    </div>\n  );\n}\n"